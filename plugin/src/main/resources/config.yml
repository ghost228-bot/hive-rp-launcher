# Порт HTTP-API (должен совпадать с guardPort в config.json лаунчера)
port: 25671
# Секрет (guardSecret из config.json лаунчера)
secret: "65550c924d9b8a2a71e6cfa141f76ce2aa60f5f91264bb29"
# true  - вход/регистрация проверяются на сервере (уникальные ники)
# false - старый режим (только пропуск по секрету), для переходного периода
require-password: true
